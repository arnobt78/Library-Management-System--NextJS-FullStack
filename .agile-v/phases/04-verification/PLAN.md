# Stage 4 Plan - Independent Verification

Red Team Verifier reads approved requirements and executes the independent test specification against synthesized artifacts. It checks external behavior, negative/boundary paths, realistic data types, security, stubs, anti-patterns, and traceability. Findings use VER IDs and FT codes; eval results and the validation summary are updated.
