# Test Specification - C1

- Status: APPROVED FOR EXECUTION (`GATE-0002`, `GATE-0003`)
- Source: `.agile-v/REQUIREMENTS.md` C1-approved.3
- Scope: REQ-0019 through REQ-0025
- Test cases: 27

| TC-ID | REQ-ID | Description | Expected | Type | Steps |
|---|---|---|---|---|---|
| TC-0019 | REQ-0019 | Compare application-managed environment keys with `.env.example` | Every executable key is documented exactly once with scope, requirement, format, placeholder, and acquisition source | integration | Extract `process.env` and config keys; compare key sets; inspect comments |
| TC-0020 | REQ-0019 | Scan the example environment file for credential material | No value resembles a real token, password, private endpoint, or personal datum | negative | Run secret-pattern scan; manually inspect placeholders |
| TC-0021 | REQ-0019 | Verify platform-managed variables are non-configurable documentation | `NODE_ENV`, `VERCEL`, and `VERCEL_URL` are described but not assigned user values | edge | Inspect the platform section and assignments |
| TC-0022 | REQ-0020 | Verify framework/runtime versions and Node constraint | Next 16.2.12, React/DOM 19.2.8, and Node >=20.9 are resolved | integration | Inspect manifests and resolved dependency tree |
| TC-0023 | REQ-0020 | Verify the Next.js request interception convention | `proxy.ts` exports the Auth.js proxy and no `middleware.ts` remains | integration | Inspect root files; run route/build compilation |
| TC-0024 | REQ-0020 | Preserve the App Router surface | All 63 page/route/layout entries and all 39 API routes remain present | regression | Count entry files before and after migration |
| TC-0025 | REQ-0020 | Exercise authenticated and unauthenticated proxy decisions | Existing public routes remain reachable and protected routes still require authentication | system | Run proxy/auth integration tests for both session states |
| TC-0026 | REQ-0021 | Perform a clean lockfile install | `npm ci` exits 0 with no peer dependency errors | system | Remove generated install state in a clean environment; execute `npm ci` |
| TC-0027 | REQ-0021 | Audit all resolved production and development dependencies | `npm audit --audit-level=low` reports zero vulnerabilities | security | Execute audit against the generated lockfile |
| TC-0028 | REQ-0021 | Verify external SDK contracts | ImageKit authentication/upload and Upstash workflow imports typecheck and preserve their public results | integration | Compile SDK call sites and exercise mocked success/failure contracts |
| TC-0029 | REQ-0021 | Verify direct dependency usage or exception rationale | Every direct package is referenced by executable/config source or justified in the Build Manifest | negative | Run dependency usage scan; compare exceptions |
| TC-0030 | REQ-0022 | Run strict TypeScript validation | `npm run typecheck` exits 0 without ignored errors | system | Execute the script in a clean install |
| TC-0031 | REQ-0022 | Run zero-warning ESLint validation | `npm run lint` invokes `eslint . --max-warnings 0` and exits 0 | system | Inspect script; execute it |
| TC-0032 | REQ-0022 | Run production compilation | `npm run build` exits 0 without framework error suppression | system | Inspect config; execute production build |
| TC-0033 | REQ-0023 | Verify mutation-key coverage by domain | Books, borrows, reviews, users, requests, fines, recommendations, analytics, config/health, and exports invalidate their related keys before success settles | integration | Execute mutation contract tests with a recording QueryClient |
| TC-0034 | REQ-0023 | Verify initiating-view behavior and rollback | Success updates or immediately refetches the initiating view; failure restores optimistic state | negative | Exercise success and rejected mutations against consumer-visible cache data |
| TC-0035 | REQ-0023 | Verify inactive and back-navigation freshness | Inactive related queries are stale and refetch on next mount/navigation without manual refresh | system | Mutate, unmount, remount/back-navigate, and assert fresh fetch |
| TC-0036 | REQ-0023 | Verify same-origin multi-tab propagation | Another tab receives a data-free invalidation signal within one second and refetches active related queries | performance | Open two same-origin contexts; mutate in one; observe the other |
| TC-0037 | REQ-0023 | Bound invalidation traffic | One mutation event does not create rebroadcast loops or duplicate domain-wide refetch storms | edge | Record channel messages and query fetch counts |
| TC-0038 | REQ-0024 | Verify migration cleanup and scope protection | No unused migration dependency/debug artifact remains and unrelated user files retain their prior state | regression | Run static scans and compare scoped git diff |

## Approved REQ-0025 delta tests

| TC-ID | REQ-ID | Description | Expected | Type | Steps |
|---|---|---|---|---|---|
| TC-0039 | REQ-0025 | Reject unauthenticated privileged server actions | No write occurs; generic unauthorized result | security | Invoke each exposed admin mutation without a session |
| TC-0040 | REQ-0025 | Reject ordinary and stale-role users | Current database role/status controls access | security | Use USER and demoted JWT fixtures against admin writes |
| TC-0041 | REQ-0025 | Reject forged actor/reviewer/user IDs | Server session identity wins; no impersonated audit field | negative | Submit another user's IDs from client inputs |
| TC-0042 | REQ-0025 | Enforce borrow/return ownership | Users affect only their own records; admins follow explicit policy | security | Attempt cross-user borrow and return |
| TC-0043 | REQ-0025 | Prevent replay and concurrent inventory drift | A transition changes inventory exactly once and never below zero/above total | concurrency | Race duplicate approvals/returns against one record/copy |
| TC-0044 | REQ-0025 | Roll back multi-table failures | Borrow record, book copies, role/request state remain consistent | integration | Inject failure between transactional writes |
| TC-0045 | REQ-0025 | Keep destructive secrets out of process arguments | Script requires environment secret and explicit target ID | security | Inspect argv contract and process invocation |

## C2 Delta Test Specification

- Cycle: C2
- Status: APPROVED FOR EXECUTION (`GATE-0006`)
- Source: `.agile-v/REQUIREMENTS.md` C2-approved.1
- Delta scope: REQ-0026 through REQ-0033
- Regression baseline: unchanged C1 REQ-0019 through REQ-0025; carry forward TC-0019 through TC-0045 without modification
- Counts: 75 delta; 27 regression; 0 retired; 102 active total

| TC-ID | REQ-ID | Description | Expected / unambiguous pass criterion | Type | Category | Origin Cycle | Executable steps |
|---|---|---|---|---|---|---|---|
| TC-0046 | REQ-0026 | Create a new versioned memory-hard credential | The stored value contains a recognized version/algorithm marker and decodes to Argon2id memory >=19456 KiB, iterations >=2, parallelism >=1, or scrypt N>=32768, r>=8, p>=3; plaintext and legacy SHA-256 are absent | security | positive | C2 | Register/change password; read the stored encoding from a disposable database; parse its algorithm parameters; verify login succeeds |
| TC-0047 | REQ-0026 | Upgrade a valid legacy credential during login | The login succeeds and, in the same committed authentication operation, the legacy value is replaced once by a compliant TC-0046 encoding | integration | positive | C2 | Seed a salted-SHA-256 user; snapshot the row; log in with the correct password; re-read the row and validate the new encoding |
| TC-0048 | REQ-0026 | Reject wrong passwords and malformed hashes without writes | Every attempt fails with the same non-secret-bearing external error and leaves credential bytes, version, and update timestamp unchanged | security | negative | C2 | Seed valid legacy/current and malformed encodings; record rows; submit wrong passwords to each; compare rows and responses before/after |
| TC-0049 | REQ-0026 | Bound credential timing leakage | Across at least 1,000 wrong-password samples per existing and nonexistent account, the 95% confidence interval for median response-time difference is <=20% of the slower median | performance | security | C2 | Warm the production build; alternate randomized existing/nonexistent requests; exclude network setup; calculate medians and bootstrap confidence interval |
| TC-0050 | REQ-0026 | Race concurrent legacy logins | All valid attempts authenticate, exactly one compliant final hash remains, no malformed/intermediate value is observable, and no request resets or corrupts the password | concurrency | boundary | C2 | Seed one legacy user; release 20 correct-password logins from a barrier; capture results and row history; validate final encoding and subsequent login |
| TC-0051 | REQ-0026 | Enforce the public/admin diagnostic disclosure boundary | Public output contains only service state, timestamp, and opaque request ID; provider config, email, topology, sizes, and raw errors are absent; an authenticated current DB admin can obtain the approved diagnostic fields | security | negative | C2 | Request healthy and forced-failure diagnostics anonymously, as USER, stale ADMIN, and current ADMIN; compare parsed keys and raw bodies |
| TC-0052 | REQ-0026 | Enforce production security headers and CSP compatibility | Every sampled page/API has `nosniff`, strict-origin-when-cross-origin, least-privilege Permissions-Policy and an enforcing CSP; HTTPS has HSTS, HTTP does not; Auth.js, ImageKit, and core routes complete with zero CSP violations that block required behavior | integration | security | C2 | Crawl the representative production route/API matrix over HTTPS and an HTTP probe; record headers/CSP reports; complete sign-in and upload flows |
| TC-0053 | REQ-0026 | Apply the pre-auth upload authorization limit at its boundary | For one normalized client key, requests 1-5 within a rolling 10-minute window are admitted, request 6 is rejected without provider authorization, and a request after expiry is admitted; equivalent textual forms normalize to one key | system | boundary | C2 | Freeze clock; issue five requests, then a sixth; inspect provider spy; advance to just before/at expiry; repeat with normalized-key variants |
| TC-0054 | REQ-0026 | Reject invalid persisted media and sanitize provider failures | Unsupported MIME, spoofed content, and values one byte above each feature size limit persist no media/reference; valid boundary files persist; provider errors expose no credential, URL secret, raw stack, or personal data | integration | negative | C2 | Upload valid-at-limit, over-limit, mismatched MIME/content, and forced-provider-failure fixtures; inspect response, provider call, and database |
| TC-0055 | REQ-0026 | Run security and authentication/media regression evidence | Secret scan finds zero credential material and the complete authentication and media regression suite exits 0 with no public disclosure or route regression | system | regression | C2 | Run the repository secret scanner and requirement-mapped auth/media suite against a clean production build; retain commands, exit codes, and report |
| TC-0056 | REQ-0027 | Prove the mutation-to-domain matrix is complete | Books, users, borrows, holds, renewals, reviews, requests, fines, recommendations, analytics, and operations each map every mutation to all dependent typed query domains and any RSC path | integration | positive | C2 | Load the exported mutation-domain registry in a contract test; compare its exact domain/mutation set to the required matrix; fail on missing/untyped entries |
| TC-0057 | REQ-0027 | Commit and project one successful mutation | One user action emits exactly one mutation request and one PostgreSQL commit; confirmed state appears in the initiating UI <=100 ms at p95 after server-result receipt across >=100 runs | performance | positive | C2 | Instrument network, DB commits, result receipt, and rendered state; execute 100 representative mutations; calculate p95 and count calls/commits |
| TC-0058 | REQ-0027 | Invalidate dependent domains before success settles | Every mapped active/inactive key is marked invalid synchronously before the mutation success promise/callback settles; unrelated keys remain valid | integration | positive | C2 | Use a recording query client and completion marker; run one mutation per domain; assert event ordering and exact affected/unaffected key sets |
| TC-0059 | REQ-0027 | Roll back a rejected optimistic mutation | On server rejection, the exact pre-mutation snapshot is restored, no success state remains, no PostgreSQL write commits, and a bounded error is visible | integration | negative | C2 | Seed visible/cache state; force a transactional rejection; capture optimistic state; await settlement; compare UI/cache/DB to the snapshot |
| TC-0060 | REQ-0027 | Prevent an older response replacing newer confirmed state | After two same-entity mutations resolve newest-first, final UI/cache/DB show the newer confirmed generation and remain unchanged when the older response arrives | concurrency | boundary | C2 | Delay response A; submit A then B; resolve B, snapshot state, then resolve A; assert generation ordering and final consumer-visible value |
| TC-0061 | REQ-0027 | Protect inactive and history navigation from known stale data | On remount/back/forward, an unpatchable invalidated query shows a dimension-stable inline data-slot fallback until one fresh fetch completes, never the known pre-mutation value as confirmed | system | negative | C2 | Prime then unmount a view; mutate related data; navigate back/forward under delayed GET; record frames, layout boxes, and fetch count |
| TC-0062 | REQ-0027 | Reconcile RSC paths without a hard reload | A mutation schedules affected RSC/path reconciliation in the background, preserves document/navigation state, and converges within one network round trip | integration | positive | C2 | Record document/navigation IDs and requests; mutate an RSC-backed view; await reconciliation; assert no document request/reload and fresh server content |
| TC-0063 | REQ-0027 | Reconcile on focus and reconnect | A stale affected view performs one fresh fetch after focus or network reconnect and displays current PostgreSQL state without manual refresh | system | integration | C2 | Prime data; mutate through a second context; background/offline the first; restore focus/network separately; assert one fetch and current rendered state |
| TC-0064 | REQ-0027 | Deliver data-free same-origin tab invalidation | The second tab receives an invalidation signal <=1 second after commit, the payload contains identifiers only and no business/personal values, and active affected data refetches once | performance | integration | C2 | Open two same-origin tabs; spy on BroadcastChannel and requests; mutate in tab A; timestamp signal/render in B; inspect serialized payload |
| TC-0065 | REQ-0027 | Bound mutation, refetch, and rebroadcast traffic | One action causes one mutation call, at most one invalidation broadcast per originating event, no echo loop, and no duplicate GET for the same active key/generation | concurrency | negative | C2 | Run one mutation with three tabs and multiple active/inactive observers; capture calls/messages for two seconds; group by event ID/key/generation |
| TC-0066 | REQ-0028 | Preserve server-first route ownership and granular streaming | Each route page remains a Server Component orchestrator; runtime data streams behind the smallest practical boundary while known shell/static structure renders independently | system | positive | C2 | Build production output; request representative routes with data delayed; capture streamed HTML frames and client bundle ownership; verify shell precedes data |
| TC-0067 | REQ-0028 | Meet slow-4G navigation feedback latency | Across >=100 representative navigations, click feedback or destination shell appears <=100 ms at p95 under the defined slow-4G profile | performance | boundary | C2 | Run production build with fixed slow-4G latency/throughput/CPU profile; mark click and first feedback/shell paint; calculate p95 |
| TC-0068 | REQ-0028 | Meet representative Core Web Vitals budgets | For the route/viewport matrix, field-equivalent samples meet p75 LCP <=2.5 s, INP <=200 ms, and CLS <=0.1 | performance | production-evidence | C2 | Execute production browser traces for each approved representative route/viewport with sufficient samples; compute p75 and retain raw reports |
| TC-0069 | REQ-0028 | Hydrate SSR data without duplicate initial GETs | Each SSR-provided query hydrates once and emits zero duplicate initial data GETs before an explicit stale/reconciliation event | integration | negative | C2 | Intercept network; hard-load each representative route; wait for hydration/idle; count GETs by normalized query key and inspect hydrated data |
| TC-0070 | REQ-0028 | Keep streamed data slots dimension-stable | Delayed, empty, error, and final states preserve the reserved inline slot and total page CLS remains <=0.1; no whole-destination spinner replaces a known shell | system | boundary | C2 | Force delayed/empty/error/success fixtures; record element rectangles and layout shifts frame-by-frame; inspect visible shell/fallback |
| TC-0071 | REQ-0028 | Bound destination prefetch | Standard links prefetch normally, dense low-probability lists do not eagerly prefetch, and measured detail intent emits at most one destination request per intent/navigation | performance | negative | C2 | Intercept requests; load normal and dense lists; idle, hover/focus measured detail links, then navigate; count each destination request |
| TC-0072 | REQ-0028 | Preserve streaming interaction, focus, and hydration integrity | Shared layout controls work while data is suspended, focus moves predictably on navigation, and the console has zero hydration mismatch/error messages | accessibility | integration | C2 | Delay route data; operate header/menu by keyboard during suspension; navigate; inspect focus and captured browser console through final render |
| TC-0073 | REQ-0028 | Honor keyboard and reduced-motion behavior | All route interactions are keyboard-operable with visible focus; under reduced motion no nonessential transition runs while feedback remains within 100 ms | accessibility | negative | C2 | Repeat route matrix using keyboard only and `prefers-reduced-motion: reduce`; inspect focus styles, animation events, and feedback timings |
| TC-0074 | REQ-0028 | Preserve responsive layout and visual contract | At 320, 768, 1024, and 1440 CSS px, no critical flow horizontally scrolls, overlaps, loses controls, or changes approved tokens/layout beyond recorded tolerances | system | regression | C2 | Capture baseline-comparable screenshots and DOM overflow at four widths for representative public/admin routes; complete primary tasks |
| TC-0075 | REQ-0029 | Reach user 360 from every supported identity surface | Every admin-table and supported dashboard/activity user label links to the matching `/admin/users/[id]`; no label resolves to another user or a dead route | system | positive | C2 | Seed distinct users/activity; enumerate supported surfaces; activate every identity link; assert route UUID and displayed identity |
| TC-0076 | REQ-0029 | Enforce user-detail authorization from current database state | Unauthenticated, ordinary cross-user, and stale/demoted-admin requests are denied; current ADMIN succeeds; ordinary users receive only the existing self-profile subset | security | negative | C2 | Request page and backing data under no session, USER self/other, stale ADMIN token, and current ADMIN; inspect status, HTML, and payload |
| TC-0077 | REQ-0029 | Validate UUID and pagination/filter inputs | Invalid UUIDs, page <=0, nonnumeric values, oversized limits, and unsupported filters return bounded validation/not-found responses with zero broad query or restricted data | security | boundary | C2 | Submit a table of invalid path/query inputs; capture response and query spy; assert capped/no query behavior and sanitized errors |
| TC-0078 | REQ-0029 | Render an empty 360 profile | A valid user with no circulation/activity renders zero-valued KPIs and explicit empty sections without errors, NaN, phantom records, or hidden navigation | system | boundary | C2 | Seed a user with no related rows; open as ADMIN; inspect every KPI/section and browser console |
| TC-0079 | REQ-0029 | Paginate 51+ records with exact bounds | Default page returns exactly 25 records, requested limit caps at 50, pages are stable/nonoverlapping, and all seeded records appear exactly once across traversal | integration | boundary | C2 | Seed 63 tie-prone timeline rows; request default, max, over-max, next/back pages; compare ordered IDs and counts |
| TC-0080 | REQ-0029 | Reconcile shared circulation KPIs to source records | `current`, `pending`, `returned`, `overdue`, `outstandingFine`, `onTimeReturnRate`, `averageLoanDays`, and `topGenres` exactly match independently calculated fixed-fixture values | integration | positive | C2 | Seed a fixture spanning every status/date/fine/genre; query source rows; calculate expected definitions; compare API and rendered DTO values |
| TC-0081 | REQ-0029 | Exclude restricted fields before unauthorized serialization | Unauthorized page/API responses and HTML contain zero restricted field names or seeded sentinel values, including embedded hydration data | security | privacy | C2 | Seed unique sentinels in restricted fields; request as unauthenticated/USER/stale ADMIN; scan raw bodies, headers, and serialized scripts |
| TC-0082 | REQ-0029 | Refresh 360 data after related mutations | Each borrow, hold, renewal, fine, review, request, and account-state mutation updates affected profile sections/KPIs under REQ-0027 without manual reload | integration | positive | C2 | Keep 360 open; perform each mutation through another supported surface; record invalidation, fetches, and exact rendered deltas |
| TC-0083 | REQ-0029 | Keep the profile usable across input and viewport modes | Table/timeline/profile are keyboard and screen-reader navigable with names/headings; at 320/768/1024/1440 px no critical content or action is lost | accessibility | boundary | C2 | Run automated semantics plus keyboard traversal at four widths; inspect focus order/names and complete pagination/actions |
| TC-0084 | REQ-0030 | Order reservations by deterministic FIFO tie-break | Queue order is ascending creation time then ID, and repeated reads/transactions produce the identical order and positions | integration | positive | C2 | Seed reservations with earlier/later and equal timestamps but shuffled IDs; fetch repeatedly and after an unrelated write; compare exact order |
| TC-0085 | REQ-0030 | Reject duplicate active reservations | A user with WAITING or READY for a book cannot create another active reservation; one active row remains and no notification/inventory change occurs | integration | negative | C2 | Seed WAITING then READY cases; submit duplicate requests and a replay; inspect rows, position, notifications, and copies |
| TC-0086 | REQ-0030 | Enforce the reservation state machine and expiry boundary | Only specified transitions succeed; READY expires at configured/default 48 hours (not one tick before, succeeds at boundary); terminal states cannot transition again | system | boundary | C2 | Freeze time; attempt all state pairs; test 48h minus one tick and exact boundary under default/custom config; inspect audit data |
| TC-0087 | REQ-0030 | Allocate a return to the earliest eligible reservation atomically | One return changes the earliest eligible WAITING to READY, does not expose the copy as freely available, and emits one idempotent notification | integration | positive | C2 | Seed borrowed copy and ordered eligible/ineligible queue; return it; inspect transactionally committed book/reservation/notification state |
| TC-0088 | REQ-0030 | Race simultaneous return and hold operations | Under barrier-released return/hold transactions, at most one reservation becomes READY, deterministic eligibility/order wins, and allocation never exceeds total copies | concurrency | boundary | C2 | Repeat concurrent return plus multiple hold creations against one copy in disposable PostgreSQL; assert rows/invariants after each run |
| TC-0089 | REQ-0030 | Fulfil a READY reservation exactly once | Eligible owner fulfilment changes READY to FULFILLED and creates one BORROWED allocation; replay creates no second borrow or inventory decrement | integration | positive | C2 | Seed one READY offer; fulfil, then replay concurrently/sequentially; inspect reservation, borrow, copies, and audit actor/time |
| TC-0090 | REQ-0030 | Renew with configured duration and maximum | A non-overdue BORROWED record with no other waiter extends by the validated configured/default duration, increments renewal count once, and succeeds only through the maximum | system | positive | C2 | Seed eligible borrow; renew through default/custom maximum; compare due dates/counts after each attempt and one beyond limit |
| TC-0091 | REQ-0030 | Deny overdue and waiting-user renewals | An overdue borrow or one with another active waiter is unchanged; no due-date/count/inventory write or notification occurs | integration | negative | C2 | Seed separate overdue and queued-waiter cases; snapshot rows; request renewals; compare DB and external results |
| TC-0092 | REQ-0030 | Roll back lifecycle failures and replays | Injected failures leave reservation, borrow, inventory, and notification state at the exact pre-transaction snapshot; retried/replayed commands apply at most once | integration | negative | C2 | Inject failure after each multi-write boundary for return/offer/fulfil/renew; retry with same command ID; compare all affected tables |
| TC-0093 | REQ-0030 | Enforce ownership, authorization, audit, and allocation invariants | Cross-user/unauthenticated actions fail; every success records server actor/time; `availableCopies >=0` and active READY+BORROWED allocations never exceed total copies | security | negative | C2 | Attempt each action as none, owner, other user, stale admin, current admin; run invariant query after successes/failures |
| TC-0094 | REQ-0030 | Refresh queue and renewal UI coherently | Successful/cancelled/expired/fulfilled/renewed states and queue positions reach affected views under REQ-0027 with no hard reload, duplicate request, or broadcast loop | integration | positive | C2 | Open user/admin and second-tab views; execute every transition; capture requests/signals and rendered state/positions |
| TC-0095 | REQ-0031 | Calculate all insights from a fixed dataset | 30-day trend, on-time rate, overdue ratio, outstanding-fine total, demand/copy ratio, hold pressure, renewal rate, and top genres equal independently calculated exact fixture values | integration | positive | C2 | Seed a versioned dataset with known dates/statuses/fines/copies/holds/renewals/genres; calculate expected values; compare profile and analytics outputs |
| TC-0096 | REQ-0031 | Handle zero denominators deterministically | Every ratio with a zero denominator returns the documented zero/null display, never NaN, Infinity, exception, or misleading percentage | unit | boundary | C2 | Seed empty/zero-copy/zero-return variants; invoke each aggregate and render both pages; inspect numeric and display values |
| TC-0097 | REQ-0031 | Enforce 30-day and return-date boundaries | Records one tick outside the documented period are excluded and records exactly on inclusive boundaries follow the displayed versioned formula/time zone | unit | boundary | C2 | Freeze clock/time zone; seed rows immediately before/at/after both period boundaries; compare included IDs and results |
| TC-0098 | REQ-0031 | Display explainability metadata | Every insight displays or links to its formula version and exact data period, matching the formula actually used | accessibility | positive | C2 | Enumerate insight cards in user 360/analytics; activate explanation by keyboard; compare label/version/period to calculated query metadata |
| TC-0099 | REQ-0031 | Keep insights advisory and side-effect free | Loading/refetching insights performs no writes and cannot change role, eligibility, fines, reservation priority, or any circulation state | security | negative | C2 | Snapshot protected tables; load/refetch with adversarial query inputs; inspect SQL/write spy and compare snapshots |
| TC-0100 | REQ-0031 | Prevent external disclosure of personal/borrowing data | Insight flows make zero requests to external AI/LLM hosts and no external payload contains personal or borrowing fields or developer-note credentials | security | privacy | C2 | Route all egress through a recording proxy; load/recompute/export insights; inspect destinations/bodies and run targeted credential/payload scan |
| TC-0101 | REQ-0031 | Bound insight queries and latency | Query count and rows scanned/returned stay within documented fixed bounds as fixture volume increases 10x; no per-user/per-row N+1 growth occurs | performance | boundary | C2 | Seed small/10x datasets; capture SQL count/plans/row counts and duration; compare against declared bounds and fail on linear query-count growth |
| TC-0102 | REQ-0031 | Invalidate stale insights after mutations | Related circulation/fine/hold/renewal mutations invalidate old aggregates synchronously and both pages converge to exact new values under REQ-0027 | integration | positive | C2 | Prime caches; mutate one input for every formula family; inspect invalidation order, fallback/current values, fetch count, and final calculations |
| TC-0103 | REQ-0031 | Remain available without an AI provider | With all AI/provider configuration absent or endpoints unreachable, deterministic insights remain complete and no provider-disabled error is shown | system | negative | C2 | Remove provider variables and block provider network; start production build; load/recompute all insights and inspect logs/UI |
| TC-0104 | REQ-0032 | Separate public liveness from operator diagnostics | Public liveness exposes only service state, timestamp, opaque request ID; anonymous/USER/stale-admin diagnostics fail; current operator access returns approved dependency state without secrets/PII/raw errors | security | negative | C2 | Probe healthy/degraded endpoints under four identities; scan raw responses with seeded sentinels and compare allowed key sets |
| TC-0105 | REQ-0032 | Record bounded route/mutation telemetry | Representative authenticated reads/mutations emit latency and success/error telemetry with route templates and bounded labels, excluding credentials/personal payloads | integration | positive | C2 | Send success/failure requests containing unique PII/secret sentinels; query telemetry export; assert metric/event fields, templates, and sentinel absence |
| TC-0106 | REQ-0032 | Calculate SLOs with the specified exclusions | Monthly calculation reports availability >=99.5%, read p95 <=1000 ms, mutation p95 <=1500 ms, error rate <1% for passing fixture; deliberate rate-limit responses do not enter server-error numerator | performance | boundary | C2 | Feed a timestamped synthetic month at each threshold plus just-over failures and 429s; recompute and compare exact status/percentiles |
| TC-0107 | REQ-0032 | Deliver critical alerts to the named owner | Injected availability/security conditions reach the recorded owner through the configured route <=5 minutes after detection and contain no credentials/personal payload | system | production-evidence | C2 | In production monitoring, record start/detection/receipt times; inject approved synthetic failures; preserve alert receipt and sanitized payload evidence |
| TC-0108 | REQ-0032 | Represent dependency degradation safely | A failed dependency changes authenticated diagnostics/telemetry to degraded while public liveness remains minimal and no raw dependency error/topology leaks | integration | negative | C2 | Fail each dependency independently; probe liveness/diagnostics, core unaffected route, telemetry, and logs; restore and confirm recovery |
| TC-0109 | REQ-0032 | Preserve security headers and rate-limit observability | Production probes satisfy REQ-0026 headers; rate-limit responses are enforced, observable separately, and excluded from server-error SLO | security | regression | C2 | Crawl production matrix; trigger approved limit; capture headers/status/metrics and recompute error rate with/without 429s |
| TC-0110 | REQ-0032 | Pass preview and production deployment smoke | Auth, catalog, representative admin read, mutation rollback-safe probe, media, health, and navigation smoke all pass against both deployed environments with version/time/URL evidence | system | production-evidence | C2 | Run the signed smoke manifest against preview then production; retain deployment IDs, timestamps, endpoints, per-step results, and exit code 0 |
| TC-0111 | REQ-0032 | Prove migration rollback procedure | Apply migration to a production-like backup clone, verify forward state, invoke documented rollback at the declared criterion, and recover the exact pre-migration schema/data checksums | integration | production-evidence | C2 | Restore clone; record checksums; apply migration and fixture writes; execute rollback; compare schema/data and application smoke |
| TC-0112 | REQ-0032 | Meet backup recovery objectives with current evidence | A backup no older than 24 hours restores successfully within 4 hours, passes integrity/application smoke, and completion evidence is dated <=30 days before acceptance | system | production-evidence | C2 | Select recorded backup; timestamp restore start/end; restore isolated target; run FK/count/checksum and application smoke; verify evidence date |
| TC-0113 | REQ-0032 | Sustain representative load without telemetry explosion | Under representative load, read/mutation p95 and error targets pass, metric cardinality stays below the documented budget, and telemetry contains no raw IDs/PII/secrets | performance | boundary | C2 | Run declared load mix/duration; export latency/errors/series count; scan label/value samples for raw identifiers and sentinels |
| TC-0114 | REQ-0032 | Validate the Gate 2 production evidence bundle | Evidence identifies named owners, dashboards, alert routes, measured SLO window/results, rollback criteria, smoke deployment IDs, and a qualifying restore; every reference resolves and is dated | system | production-evidence | C2 | Run an evidence-schema/link audit; open each referenced record; compare dates/owners/results to REQ-0032 and fail on missing/stale items |
| TC-0115 | REQ-0033 | Relocate utilities without losing routes | Desktop primary navigation contains no API Docs/Status/Performance entries; profile utility menu and mobile drawer contain each exactly once and all destinations still resolve | system | positive | C2 | Inspect rendered authenticated desktop/mobile nav; count labels/links; activate each entry; assert final route and successful content |
| TC-0116 | REQ-0033 | Keep every utility within two interactions | From any authenticated primary page at desktop/mobile widths, each utility destination is reached in <=2 keyboard or pointer activations | accessibility | boundary | C2 | Start on representative pages; reset between utilities; count activations to open menu/drawer and select destination; record route |
| TC-0117 | REQ-0033 | Validate keyboard, names, focus, active, and close behavior | Menu/drawer entries have unique screen-reader names and visible focus; Enter/Space activates; Escape/outside navigation closes appropriately; active destination is programmatically exposed | accessibility | positive | C2 | Run keyboard/screen-reader semantic traversal; invoke open/close/activate paths; inspect focus target and accessibility tree states |
| TC-0118 | REQ-0033 | Preserve navigation at required widths | At 320, 768, 1024, and 1440 CSS px, utilities are discoverable, menus stay in viewport without horizontal page scroll/overlap, and primary library tasks remain present | system | boundary | C2 | Set each viewport; open all nav surfaces; capture geometry/overflow; activate every library and utility link |
| TC-0119 | REQ-0033 | Honor optional motion duration and reduced motion | Any ripple/reveal/parallax lasts 150-300 ms normally and emits no nonessential animation under reduced motion; interaction and navigation still complete | accessibility | negative | C2 | Record animation events/timings with normal and reduced-motion media settings; activate all modified interactions and compare |
| TC-0120 | REQ-0033 | Preserve media fallback, reuse, performance, and route behavior | Safe media failures render an accessible stable fallback; repeated modified behavior uses one shared primitive/list; no added layout shift, INP remains <=200 ms, and all prior utility routes pass regression | system | regression | C2 | Force missing/invalid/slow media; inspect accessible fallback/geometry; run duplicate-implementation static check, production CLS/INP trace, and route smoke |
| TC-0121 | REQ-0027 | Drive all client invalidation through the mutation registry | Every supported mutation maps to one registered family and invalidates its typed dependencies without a duplicate broadcast loop | unit | regression | C2 | Exercise hooks/consumers and compare calls/domain sets |
| TC-0122 | REQ-0027 | Reconcile every registered RSC path | All ten families call every registered path, including user 360; dynamic patterns use page scope | unit | positive | C2 | Mock `revalidatePath`; iterate registry and compare exact calls |
| TC-0123 | REQ-0030 | Deliver reservation events retry-safely | READY sends use the stable key; success stores receipt, failure retries/dead-letters within provider retention, and stale status cannot race delivery | integration | boundary | C2 | Exercise provider success/timeout, dispatch-lease transition guard and terminal attempts |
| TC-0124 | REQ-0030 | Prevent duplicate worker claims and unauthorized recovery | Production claim code returns an event once under concurrency; absent/wrong cron bearer fails closed | concurrency | security | C2 | Run concurrent production claims in disposable PostgreSQL and probe cron authorization |
