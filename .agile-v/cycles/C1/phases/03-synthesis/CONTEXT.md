# Stage 3 Context

Loaded for approved `REQ-0025` under `GATE-0003`. Preserve existing UI contracts and TanStack Query invalidation while securing Next.js server actions/API boundaries with current database identity, ownership checks, and PostgreSQL transactions. Primary implementation domain: Next.js/React/TypeScript via `build-agent-js`; risk R2.
