/**
 * Canonical surface padding for admin panels + root glass cards.
 * Keep in sync with `.admin-panel` / `.surface-card` in CSS.
 * Parent: CR-0003 / REQ-0034
 */
export const CARD_PAD_CLASS = "p-2 sm:p-4";
